# Скрипты для работы с OpenAPI

## 📋 Доступные скрипты

### `sync-api.js` - Полная синхронизация API

Главный скрипт для синхронизации API контракта.

**Использование:**
```bash
npm run sync:api
```

**Что делает:**
1. Загружает OpenAPI спецификацию с сервера
2. Валидирует спецификацию
3. Генерирует TypeScript типы напрямую с сервера (без сохранения файла)
4. Проверяет типы на ошибки

**Опции:**
- `--url <api-url>` - URL API сервера
- `--skip-check` - Пропустить проверку типов
- `--save-file` - Сохранить openapi.json в корень проекта (опционально)

**Примеры:**
```bash
# Стандартная синхронизация
npm run sync:api

# Без проверки типов (быстрее)
npm run sync:api -- --skip-check

# С сохранением файла openapi.json
npm run sync:api -- --save-file

# С кастомным URL
npm run sync:api -- --url http://api.example.com
```

### `validate-openapi.js` - Валидация OpenAPI

Проверяет валидность OpenAPI спецификации.

**Использование:**
```bash
npm run validate:openapi [url-or-path]
```

**Примеры:**
```bash
# Валидация с сервера (по умолчанию)
npm run validate:openapi

# Валидация локального файла (если сохранен с --save-file)
# npm run validate:openapi ../openapi.json  # Опционально, если файл сохранен

# Валидация с кастомного URL
npm run validate:openapi http://api.example.com/openapi.json
```

## 🎯 Преимущества подхода

### Без локального файла openapi.json

✅ **Преимущества:**
- Не нужно хранить файл в git
- Всегда актуальная версия с сервера
- Меньше конфликтов в git
- Проще для монорепо с отдельными репозиториями

### Скрипты в client

✅ **Преимущества:**
- Скрипты в том же репозитории, где используются типы
- Не нужен доступ к корню проекта
- Проще для CI/CD
- Каждый репозиторий самодостаточен

## 🔄 Workflow

```bash
# 1. Запустить сервер (в другом терминале)
cd ../server && npm run start:dev

# 2. Синхронизировать API (в client)
cd client
npm run sync:api

# 3. Исправить ошибки (если есть)
# ... редактируем код ...

# 4. Коммит
git add src/types/api.ts
git commit -m "Update API types"
```

## 📝 Примечания

- Скрипты работают напрямую с сервером, не требуя локального файла
- При необходимости можно использовать `--save-file` для сохранения файла
- Все скрипты используют переменную окружения `NEXT_PUBLIC_API_URL` или `http://localhost:3001` по умолчанию
