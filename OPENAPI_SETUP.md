# 🔄 Настройка автоматизации OpenAPI (без локального файла)

## 🎯 Особенности

✅ **Работает без локального файла `openapi.json`**
- Генерация типов напрямую с сервера
- Не нужно хранить файл в git
- Всегда актуальная версия

✅ **Скрипты в репозитории client**
- Все скрипты в `client/scripts/`
- Не нужен доступ к корню проекта
- Самодостаточный репозиторий

## 🚀 Быстрый старт

### Синхронизация API

```bash
# Из директории client
npm run sync:api
```

Эта команда:
1. 📥 Загружает OpenAPI спецификацию с сервера
2. ✅ Валидирует спецификацию
3. 🔧 Генерирует TypeScript типы
4. ✅ Проверяет типы на ошибки

## 📋 Доступные команды

```bash
# Полная синхронизация (рекомендуется)
npm run sync:api

# Синхронизация без проверки типов (быстрее)
npm run sync:api -- --skip-check

# С сохранением файла openapi.json (опционально)
npm run sync:api -- --save-file

# Валидация OpenAPI
npm run validate:openapi

# Генерация только типов (без проверки)
npm run generate:api-types
```

## 🔄 Workflow

### Стандартный сценарий

```bash
# 1. Запустить сервер (в другом терминале)
cd ../server
npm run start:dev

# 2. Синхронизировать API (в client)
cd ../client
npm run sync:api

# 3. Исправить ошибки (если есть)
# ... редактируем код ...

# 4. Коммит
git add src/types/api.ts
git commit -m "Update API types"
```

### С кастомным URL

```bash
npm run sync:api -- --url http://api.example.com
```

## ⚙️ Настройка

### URL сервера

Скрипты автоматически определяют URL:

1. **Переменная окружения**: `NEXT_PUBLIC_API_URL`
2. **Аргумент командной строки**: `--url <api-url>`
3. **По умолчанию**: `http://localhost:4000`

### Переменные окружения

Создайте `.env.local` в `client/`:

```env
NEXT_PUBLIC_API_URL=http://localhost:4000
```

## 📁 Структура

```
client/
├── scripts/
│   ├── sync-api.js          # Главный скрипт синхронизации
│   ├── validate-openapi.js  # Валидация OpenAPI
│   └── README.md            # Документация скриптов
├── src/
│   └── types/
│       └── api.ts           # Автогенерируемые типы
└── package.json
```

## ✅ Преимущества подхода

### Без локального файла

- ✅ Не нужно хранить `openapi.json` в git
- ✅ Всегда актуальная версия с сервера
- ✅ Меньше конфликтов в git
- ✅ Проще для монорепо с отдельными репозиториями

### Скрипты в client

- ✅ Скрипты в том же репозитории, где используются типы
- ✅ Не нужен доступ к корню проекта
- ✅ Проще для CI/CD
- ✅ Каждый репозиторий самодостаточен

## 🐛 Troubleshooting

### Ошибка: "Сервер недоступен"

```bash
❌ Ошибка: Ошибка сети: connect ECONNREFUSED
```

**Решение:**
1. Убедитесь, что сервер запущен: `cd ../server && npm run start:dev`
2. Проверьте URL в `NEXT_PUBLIC_API_URL`
3. Проверьте, что сервер отдает `/openapi.json`

### Ошибка: "Таймаут запроса"

**Решение:**
- Проверьте, что сервер запущен и доступен
- Увеличьте таймаут в скрипте (если нужно)

### Ошибки типов после синхронизации

Это нормально! API контракт изменился, нужно обновить код:

1. Изучите ошибки: `npm run tscheck`
2. Обновите код для соответствия новому контракту
3. Повторите проверку

## 📚 Дополнительная документация

- [scripts/README.md](./scripts/README.md) - документация скриптов
- [OPENAPI_TYPES.md](./OPENAPI_TYPES.md) - работа с типами API
- [API_TYPES_SETUP.md](./API_TYPES_SETUP.md) - настройка типов

---

**Готово к использованию!** 🎉
