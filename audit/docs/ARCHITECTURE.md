# Архитектура проекта Dohkar

## 📁 Структура проекта

```
dohkar/client/
├── app/                      # Next.js App Router
│   ├── layout.tsx           # Корневой layout
│   ├── page.tsx             # Главная страница
│   └── globals.css          # Глобальные стили
├── src/
│   ├── components/          # React компоненты
│   │   ├── ui/             # Базовые UI компоненты (Button, Input, Card)
│   │   ├── layout/         # Layout компоненты (Header, Footer, Container)
│   │   └── features/       # Feature-специфичные компоненты
│   ├── lib/                # Библиотеки и утилиты
│   │   ├── utils/          # Утилиты (cn, format, validation)
│   │   └── api-client.ts   # API клиент
│   ├── hooks/              # Custom React хуки
│   │   ├── use-local-storage.ts
│   │   ├── use-debounce.ts
│   │   └── use-media-query.ts
│   ├── types/              # TypeScript типы
│   │   └── common.ts       # Общие типы
│   ├── services/           # API сервисы
│   │   └── example.service.ts
│   ├── constants/          # Константы приложения
│   │   ├── config.ts       # Конфигурация
│   │   └── routes.ts       # Маршруты
│   ├── config/             # Конфигурация
│   │   └── env.ts          # Переменные окружения
│   └── styles/             # Дополнительные стили
├── public/                 # Статические файлы
└── package.json

```

## 🏗️ Слои архитектуры

### 1. Presentation Layer (Компоненты)
- **UI Components**: Переиспользуемые UI компоненты без бизнес-логики
- **Layout Components**: Компоненты структуры страницы (Header, Footer)
- **Feature Components**: Компоненты с бизнес-логикой

### 2. Business Logic Layer
- **Hooks**: Custom хуки для переиспользуемой логики
- **Services**: Бизнес-логика и работа с API
- **Utils**: Вспомогательные функции

### 3. Data Layer
- **API Client**: Централизованная работа с HTTP запросами
- **Types**: Типы данных и интерфейсы

## 🎯 Принципы

1. **Separation of Concerns**: Разделение ответственности между слоями
2. **DRY (Don't Repeat Yourself)**: Переиспользование кода
3. **Single Responsibility**: Каждый модуль отвечает за одну задачу
4. **Type Safety**: Строгая типизация с TypeScript

## 📦 Основные компоненты

### API Client
Централизованный клиент для работы с API:
```typescript
import { apiClient } from '@/lib/api-client';

const data = await apiClient.get('/endpoint');
```

### Services
Сервисы для работы с конкретными доменами:
```typescript
import { exampleService } from '@/services/example.service';

const result = await exampleService.getData();
```

### Hooks
Переиспользуемые хуки:
```typescript
import { useLocalStorage, useDebounce } from '@/hooks';

const [value, setValue] = useLocalStorage('key', 'default');
```

### Components
UI компоненты:
```typescript
import { Button, Card, Input } from '@/components/ui';

<Button variant="primary">Нажми меня</Button>
```

## 🔧 Конфигурация

### Пути импорта (tsconfig.json)
```json
{
  "paths": {
    "@/*": ["./src/*"],
    "@/components/*": ["./src/components/*"],
    "@/lib/*": ["./src/lib/*"],
    // ... другие пути
  }
}
```

### Переменные окружения
Создайте файл `.env.local`:
```env
NEXT_PUBLIC_API_URL=http://localhost:3000/api
```

## 📝 Best Practices

1. **Именование файлов**:
   - Компоненты: PascalCase (Button.tsx)
   - Утилиты: kebab-case (api-client.ts)
   - Хуки: use-hook-name.ts

2. **Структура компонентов**:
   - Один компонент на файл
   - Экспорт через index.ts

3. **TypeScript**:
   - Используйте интерфейсы для объектов
   - Типизируйте все функции
   - Избегайте `any`

4. **Стили**:
   - Используйте Tailwind CSS
   - Утилита `cn()` для объединения классов

## 🚀 Расширение архитектуры

### Добавление нового сервиса
1. Создайте файл в `src/services/`
2. Используйте `apiClient` для запросов
3. Экспортируйте методы сервиса

### Добавление нового компонента
1. Создайте файл в соответствующей папке
2. Добавьте типы для пропсов
3. Экспортируйте через index.ts

### Добавление нового хука
1. Создайте файл в `src/hooks/`
2. Следуйте соглашению `use-*`
3. Добавьте JSDoc комментарии
