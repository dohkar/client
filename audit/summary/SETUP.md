# 🚀 Инструкция по установке и запуску проекта

## Предварительные требования

- Node.js 18.x или выше
- npm или yarn или pnpm

## 📥 Установка

### 1. Установите зависимости

```bash
npm install
```

### 2. Настройте переменные окружения

Скопируйте содержимое файла `env.example.txt` в новый файл `.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NODE_ENV=development
```

## 🏃 Запуск

### Режим разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Production сборка

```bash
# Создать production сборку
npm run build

# Запустить production сервер
npm run start
```

### Линтинг

```bash
npm run lint
```

## 📁 Структура проекта

```
dohkar/client/
├── app/                          # Next.js App Router
│   ├── layout.tsx               # Корневой layout с Header и Footer
│   ├── page.tsx                 # Главная страница
│   ├── about/                   # Страница "О нас"
│   ├── contact/                 # Страница "Контакты"
│   ├── api/                     # API Routes
│   │   ├── health/             # Health check endpoint
│   │   └── example/            # Пример CRUD API
│   └── globals.css             # Глобальные стили
│
├── src/
│   ├── components/             # React компоненты
│   │   ├── ui/                # Базовые UI компоненты
│   │   │   ├── Button.tsx    # Кнопка с вариантами
│   │   │   ├── Input.tsx     # Поле ввода с валидацией
│   │   │   ├── Card.tsx      # Карточка с секциями
│   │   │   └── index.ts      # Экспорты
│   │   ├── layout/           # Layout компоненты
│   │   │   ├── Header.tsx    # Шапка сайта
│   │   │   ├── Footer.tsx    # Подвал сайта
│   │   │   ├── Container.tsx # Контейнер для контента
│   │   │   └── index.ts      # Экспорты
│   │   └── features/         # Feature компоненты
│   │
│   ├── lib/                   # Библиотеки и утилиты
│   │   ├── api-client.ts     # HTTP клиент
│   │   └── utils/            # Утилиты
│   │       ├── cn.ts         # Объединение классов
│   │       ├── format.ts     # Форматирование данных
│   │       ├── validation.ts # Валидация
│   │       └── index.ts      # Экспорты
│   │
│   ├── hooks/                # Custom React хуки
│   │   ├── use-local-storage.ts  # Работа с localStorage
│   │   ├── use-debounce.ts       # Дебаунсинг
│   │   ├── use-media-query.ts    # Медиа-запросы
│   │   └── index.ts              # Экспорты
│   │
│   ├── types/                # TypeScript типы
│   │   ├── common.ts         # Общие типы
│   │   └── index.ts          # Экспорты
│   │
│   ├── services/             # API сервисы
│   │   └── example.service.ts # Пример сервиса
│   │
│   ├── constants/            # Константы
│   │   ├── config.ts         # Конфигурация приложения
│   │   ├── routes.ts         # Маршруты
│   │   └── index.ts          # Экспорты
│   │
│   ├── config/               # Конфигурация
│   │   ├── env.ts            # Переменные окружения
│   │   └── index.ts          # Экспорты
│   │
│   └── styles/               # Дополнительные стили
│
├── public/                   # Статические файлы
├── package.json             # Зависимости и скрипты
├── tsconfig.json            # Конфигурация TypeScript
├── next.config.ts           # Конфигурация Next.js
├── tailwind.config.js       # Конфигурация Tailwind CSS
├── README.md                # Основная документация
├── ARCHITECTURE.md          # Описание архитектуры
├── API_GUIDE.md             # Руководство по API
└── SETUP.md                 # Эта инструкция
```

## 🎯 Первые шаги

### 1. Изучите структуру

Ознакомьтесь с документацией:
- [README.md](./README.md) - Общее описание проекта
- [ARCHITECTURE.md](./ARCHITECTURE.md) - Архитектура проекта
- [API_GUIDE.md](./API_GUIDE.md) - Работа с API

### 2. Проверьте работу приложения

После запуска в режиме разработки, откройте:
- [http://localhost:3000](http://localhost:3000) - Главная страница
- [http://localhost:3000/about](http://localhost:3000/about) - Страница "О нас"
- [http://localhost:3000/contact](http://localhost:3000/contact) - Страница "Контакты"
- [http://localhost:3000/api/health](http://localhost:3000/api/health) - Health check API

### 3. Начните разработку

1. **Создайте новую страницу**
   ```bash
   # Создайте файл app/your-page/page.tsx
   ```

2. **Создайте новый компонент**
   ```bash
   # Создайте файл src/components/ui/YourComponent.tsx
   ```

3. **Создайте API endpoint**
   ```bash
   # Создайте файл app/api/your-endpoint/route.ts
   ```

4. **Создайте сервис**
   ```bash
   # Создайте файл src/services/your-service.service.ts
   ```

## 🔧 Полезные команды

```bash
# Разработка
npm run dev              # Запустить dev сервер

# Production
npm run build            # Создать production сборку
npm run start            # Запустить production сервер

# Качество кода
npm run lint             # Проверить код линтером

# Зависимости
npm install              # Установить зависимости
npm install <package>    # Установить пакет
npm update               # Обновить зависимости
```

## 🎨 Кастомизация

### Изменение темы

Редактируйте `app/globals.css` и `tailwind.config.js`:

```css
/* app/globals.css */
:root {
  --primary-color: #3b82f6;
  --secondary-color: #6b7280;
}
```

### Изменение конфигурации

```typescript
// src/constants/config.ts
export const APP_CONFIG = {
  name: 'Ваше название',
  description: 'Ваше описание',
};
```

### Добавление маршрутов

```typescript
// src/constants/routes.ts
export const ROUTES = {
  home: '/',
  yourPage: '/your-page',
  // ...
};
```

## 📝 Рекомендации

1. **Следуйте структуре проекта**
   - Компоненты в `src/components/`
   - Утилиты в `src/lib/`
   - API сервисы в `src/services/`

2. **Используйте TypeScript**
   - Типизируйте все данные
   - Избегайте `any`

3. **Следуйте соглашениям**
   - Компоненты: PascalCase
   - Утилиты: kebab-case
   - Хуки: use-hook-name

4. **Документируйте код**
   - Добавляйте JSDoc комментарии
   - Описывайте сложную логику

## ❓ Частые вопросы

### Как добавить новую зависимость?

```bash
npm install <package-name>
```

### Как изменить порт?

```bash
npm run dev -- -p 4000
```

Или создайте файл `.env.local`:
```env
PORT=4000
```

### Ошибка "Module not found"?

Убедитесь, что:
1. Зависимости установлены (`npm install`)
2. Пути импорта корректны (используйте `@/`)
3. Файл существует

### Как добавить переменную окружения?

1. Добавьте в `.env.local`
2. Добавьте в `src/config/env.ts`
3. Перезапустите сервер разработки

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте документацию
2. Убедитесь, что все зависимости установлены
3. Перезапустите сервер разработки
4. Очистите кэш: `rm -rf .next node_modules && npm install`

## 🎉 Готово!

Теперь вы готовы к разработке. Удачи! 🚀
