---
alwaysApply: true
---

# Правила разработки фронтенда Dohkar (Next.js)

## 🎯 Общие принципы

### Архитектура проекта
- **Фреймворк**: Next.js 14+ (App Router)
- **Стиль кода**: TypeScript strict mode, функциональный подход
- **UI библиотека**: shadcn/ui + Tailwind CSS
- **State Management**: Zustand для глобального состояния
- **Data Fetching**: React Query (TanStack Query)
- **Валидация**: Zod для схем валидации
- **Форматирование**: Prettier + ESLint

### Структура проекта
```
client/
  app/              # Next.js App Router страницы
    (routes)/       # Группировка роутов
    layout.tsx      # Root layout
  src/
    components/     # React компоненты
      ui/           # Базовые UI компоненты (shadcn)
      layout/       # Компоненты layout
      features/     # Feature-специфичные компоненты
    services/       # API сервисы
    stores/         # Zustand stores
    hooks/          # Custom React hooks
    lib/            # Утилиты и хелперы
      utils/        # Утилитные функции
      api-client.ts # API клиент
    types/          # TypeScript типы и интерфейсы
    constants/      # Константы приложения
  public/           # Статические файлы
```

## 📝 TypeScript Best Practices

### Типизация
- **ЗАПРЕЩЕНО** использовать `any` - всегда используй конкретные типы или `unknown`
- Используй `interface` для объектов, `type` для union/intersection типов
- Всегда типизируй параметры функций и возвращаемые значения
- Используй дженерики для переиспользуемых компонентов и функций
- Экспортируй типы из `@/types` для переиспользования

```typescript
// ✅ Хорошо
interface UserData {
  id: string;
  name: string;
}

function getUser(id: string): Promise<UserData> {
  // ...
}

// ❌ Плохо
function getUser(id: any): any {
  // ...
}
```

### Обработка ошибок
- Всегда используй `try-catch` для async операций
- Типизируй ошибки: `error instanceof Error`
- Используй типы `ApiError`, `ExtendedError` из `@/types`
- Не используй `console.error` в production - создай утилиту для логирования

```typescript
// ✅ Хорошо
try {
  await someAsyncOperation();
} catch (error) {
  if (error instanceof Error) {
    toast.error(error.message);
  } else {
    toast.error("Произошла неизвестная ошибка");
  }
}

// ❌ Плохо
try {
  await someAsyncOperation();
} catch (error: any) {
  console.error(error);
}
```

## 🎨 Next.js и React Best Practices

### App Router
- Используй Server Components по умолчанию
- Используй Client Components (`"use client"`) только когда нужна интерактивность
- Используй `async/await` в Server Components для данных
- Используй `generateMetadata` для SEO

```typescript
// ✅ Хорошо - Server Component
export default async function PropertyPage({ params }: { params: { id: string } }) {
  const property = await propertyService.getPropertyById(params.id);

  return (
    <div>
      <h1>{property.title}</h1>
      <PropertyDetails property={property} />
    </div>
  );
}

// ✅ Хорошо - Client Component для интерактивности
"use client";
export function PropertyDetails({ property }: { property: Property }) {
  const [isFavorite, setIsFavorite] = useState(false);
  // ...
}
```

### Компоненты
- Используй функциональные компоненты с хуками
- Разбивай большие компоненты на мелкие, переиспользуемые
- Используй `useCallback` и `useMemo` для оптимизации производительности
- Всегда типизируй props через `interface` или `type`
- Разделяй Server и Client Components

```typescript
// ✅ Хорошо
interface ButtonProps {
  label: string;
  onClick: () => void;
  variant?: "primary" | "secondary";
}

export function Button({ label, onClick, variant = "primary" }: ButtonProps) {
  const handleClick = useCallback(() => {
    onClick();
  }, [onClick]);

  return <button onClick={handleClick}>{label}</button>;
}

// ❌ Плохо
export function Button(props: any) {
  return <button onClick={props.onClick}>{props.label}</button>;
}
```

### Хуки
- Создавай custom hooks для переиспользуемой логики
- Используй `useQuery` и `useMutation` из React Query для работы с API
- Всегда обрабатывай состояния loading и error

```typescript
// ✅ Хорошо
export function useProperty(id: string) {
  return useQuery({
    queryKey: ["property", id],
    queryFn: () => propertyService.getPropertyById(id),
    enabled: !!id,
  });
}
```

### State Management
- Используй Zustand для глобального UI состояния (модалки, меню, тема)
- Локальное состояние - через `useState`
- Серверное состояние - через React Query
- Не дублируй состояние между Zustand и React Query
- Используй Server Components для данных, которые не требуют реактивности

```typescript
// ✅ Хорошо - Zustand для UI состояния
const { isMobileMenuOpen, setMobileMenuOpen } = useUIStore();

// ✅ Хорошо - React Query для серверных данных
const { data, isLoading } = useQuery({
  queryKey: ["property", id],
  queryFn: () => propertyService.getPropertyById(id),
});
```

## 🔌 API и сервисы

### Структура сервисов
- Один сервис на домен (auth, property, favorites)
- Все методы возвращают типизированные `ApiResponse<T>`
- Используй `apiClient` из `@/lib/api-client` для всех запросов
- Всегда обрабатывай ошибки
- Используй адаптеры для преобразования данных бэкенда в формат фронтенда

```typescript
// ✅ Хорошо
export const propertyService = {
  async getPropertyById(id: string): Promise<ApiResponse<Property>> {
    const response = await apiClient.get<ApiResponse<PropertyBackend>>(
      API_ENDPOINTS.properties.getById(id)
    );

    if (response.status === "success" && response.data) {
      return {
        ...response,
        data: adaptProperty(response.data), // Адаптация данных
      };
    }

    throw new Error(response.message || "Ошибка загрузки объявления");
  },
};
```

```typescript
// ✅ Хорошо
export const propertyService = {
  async getPropertyById(id: string): Promise<ApiResponse<Property>> {
    const response = await apiClient.get<ApiResponse<PropertyBackend>>(
      API_ENDPOINTS.properties.getById(id)
    );

    if (response.status === "success" && response.data) {
      return {
        ...response,
        data: adaptProperty(response.data),
      };
    }

    throw new Error(response.message || "Ошибка загрузки объявления");
  },
};
```

### Константы
- Все эндпоинты в `@/constants/routes` (API_ENDPOINTS)
- Все маршруты в `@/constants/routes` (ROUTES)
- Используй константы вместо хардкода строк

## 🎯 Стиль кода

### Именование
- Компоненты: PascalCase (`UserProfile`, `PropertyCard`)
- Функции/переменные: camelCase (`getUserData`, `isLoading`)
- Константы: UPPER_SNAKE_CASE (`API_URL`, `MAX_RETRIES`)
- Типы/интерфейсы: PascalCase (`UserData`, `ApiResponse`)
- Файлы: kebab-case для компонентов, camelCase для утилит

### Форматирование
- Используй Prettier (printWidth: 90)
- Двойные кавычки для строк в JSX, одинарные в TypeScript
- Точка с запятой обязательна
- Trailing comma в объектах и массивах

### Комментарии
- Используй JSDoc для экспортируемых функций и компонентов
- Комментарии на русском языке для бизнес-логики
- Избегай очевидных комментариев

```typescript
/**
 * Получает данные пользователя по ID
 * @param id - Уникальный идентификатор пользователя
 * @returns Promise с данными пользователя
 */
export async function getUserById(id: string): Promise<User> {
  // ...
}
```

## 🚫 Антипаттерны (НЕ ДЕЛАТЬ)

### Запрещено
- ❌ Использовать `any` тип
- ❌ Использовать `console.log/error` в production коде
- ❌ Хардкодить строки (URLs, тексты) - используй константы
- ❌ Создавать компоненты без типизации props
- ❌ Игнорировать ошибки (всегда обрабатывай)
- ❌ Дублировать код - создавай переиспользуемые функции
- ❌ Использовать `var` - только `const` и `let`
- ❌ Создавать компоненты > 300 строк - разбивай на части

### Избегай
- ⚠️ Прямого манипулирования DOM (используй React)
- ⚠️ Inline стилей (используй Tailwind классы)
- ⚠️ Глобальных переменных (используй Zustand или Context)
- ⚠️ Синхронных операций в useEffect (используй async функции)

## 🧪 Тестирование

### Приоритеты
- Критичная бизнес-логика должна быть покрыта тестами
- API сервисы должны иметь моки для тестирования
- Компоненты тестируются через интеграционные тесты

## 🔒 Безопасность

### Обязательно
- Валидируй все пользовательские данные (клиент + сервер)
- Используй типизацию для предотвращения инъекций
- Не храни чувствительные данные в localStorage
- Используй HTTPS для всех API запросов
- Санитизируй данные перед отображением

## 📦 Импорты

### Порядок импортов
1. React и Next.js
2. Сторонние библиотеки
3. Внутренние компоненты UI
4. Сервисы и утилиты
5. Типы
6. Константы
7. Стили (если есть)

```typescript
// ✅ Хорошо
import { useState, useEffect } from "react";
import { useRouter } from "next/navigation";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { propertyService } from "@/services/property.service";
import type { Property } from "@/types/property";
import { ROUTES } from "@/constants";
```

### Алиасы путей
- Используй `@/` для `src/`
- Используй относительные пути только для локальных импортов

## 🎨 UI/UX Guidelines

### Доступность
- Всегда используй семантические HTML теги
- Добавляй `aria-label` для иконок без текста
- Обеспечивай keyboard navigation
- Минимальный размер touch target: 44x44px

### Responsive Design
- Mobile-first подход
- Используй Tailwind breakpoints: `sm:`, `md:`, `lg:`, `xl:`
- Тестируй на разных размерах экранов

### Производительность
- Используй `next/image` для изображений (автоматическая оптимизация)
- Ленивая загрузка компонентов через `dynamic import` с `ssr: false` для клиентских компонентов
- Оптимизируй bundle size (анализируй через `@next/bundle-analyzer`)
- Используй `useMemo` и `useCallback` только когда это действительно нужно
- Используй Server Components для уменьшения JavaScript bundle
- Кешируй данные через React Query (`staleTime`, `cacheTime`)

```typescript
// ✅ Хорошо - Ленивая загрузка
const PropertyForm = dynamic(() => import("@/components/features/PropertyForm"), {
  ssr: false,
  loading: () => <Spinner />,
});

// ✅ Хорошо - Оптимизация изображений
import Image from "next/image";
<Image src={property.image} alt={property.title} width={800} height={600} />
```

## 🔄 Git и коммиты

### Commit messages
- **Формат**: `тип: краткое описание изменений`
- **Стиль**: Лаконичный, понятный, на английском языке
- **Типы**:
  - `feat` - новая функциональность
  - `fix` - исправление бага
  - `refactor` - рефакторинг кода
  - `style` - изменения форматирования
  - `docs` - изменения документации
  - `test` - добавление/изменение тестов
  - `chore` - обновление зависимостей, конфигурации

### Примеры хороших commit messages
```bash
# ✅ Хорошо - лаконично и понятно
refactor: improve type safety in api-client
feat: add search input component
fix: resolve mobile menu scroll issue
refactor: replace console.error with logger utility

# ❌ Плохо - слишком длинно или неясно
fix: исправил баг с меню на мобильных
refactor: улучшил код компонента Header
feat: добавил новую функцию поиска
```

### Правила
- Максимум 50-72 символа в первой строке
- Используй императив ("add" вместо "added", "fix" вместо "fixed")
- Описывай ЧТО изменилось, а не ПОЧЕМУ
- Если нужно больше контекста - добавь тело коммита (пустая строка + описание)

## 🔄 Next.js Специфичные практики

### Роутинг
- Используй App Router (не Pages Router)
- Используй `layout.tsx` для общих layout компонентов
- Используй `loading.tsx` для loading states
- Используй `error.tsx` для error boundaries
- Используй `not-found.tsx` для 404 страниц

### Метаданные и SEO
- Используй `generateMetadata` для динамических метаданных
- Используй `metadata` объект для статических метаданных
- Всегда добавляй `alt` к изображениям

```typescript
// ✅ Хорошо
export async function generateMetadata({ params }: { params: { id: string } }) {
  const property = await propertyService.getPropertyById(params.id);

  return {
    title: property.title,
    description: property.description,
    openGraph: {
      images: [property.image],
    },
  };
}
```

### Обработка данных
- Используй Server Components для получения данных когда возможно
- Используй React Query для клиентских данных и кеширования
- Используй `revalidate` для ISR (Incremental Static Regeneration)

## 📚 Дополнительные ресурсы

### Документация
- Next.js: https://nextjs.org/docs
- React Query: https://tanstack.com/query
- Zustand: https://docs.pmnd.rs/zustand
- Tailwind CSS: https://tailwindcss.com/docs
- shadcn/ui: https://ui.shadcn.com

### Внутренние конвенции
- Всегда проверяй существующие паттерны перед созданием нового
- Следуй структуре существующих компонентов
- Используй существующие утилиты и хелперы
- Используй `logger` из `@/lib/utils/logger` вместо `console.log`

---

## 🔍 Проверка типов после изменений

### Обязательная проверка
- **ВСЕГДА** после каждого code change проверяй типы TypeScript
- Используй `npx tsc --noEmit` в директории `client/`
- Исправляй все ошибки типов перед завершением работы
- Не коммить код с ошибками типов

```bash
# Проверка типов для клиента
cd client && npx tsc --noEmit
```

### Важно
- Проверка типов должна быть последним шагом перед завершением изменений
- Если есть ошибки типов - исправь их немедленно
- Не пропускай проверку типов даже для небольших изменений

## 🎯 Приоритеты при разработке

1. **Типобезопасность** - всегда используй TypeScript правильно + проверка после изменений
2. **Обработка ошибок** - никогда не игнорируй ошибки
3. **Производительность** - оптимизируй рендеринг и запросы, используй Server Components
4. **Доступность** - делай интерфейс доступным для всех
5. **Читаемость кода** - пиши код, понятный другим разработчикам
6. **SEO** - оптимизируй метаданные и структуру страниц

## ⚠️ Важные замечания

### Не создавай лишние файлы
- ❌ Не создавай MD файлы (README, документация) без явного запроса
- ❌ Не создавай временные файлы для отчетов
- ❌ Не создавай файлы, которые не являются частью кода приложения
- ✅ Создавай только файлы, необходимые для функциональности
- ✅ Используй комментарии в коде вместо отдельных файлов документации

**Причина:** Лишние файлы тратят токены и засоряют репозиторий
