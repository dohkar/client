# Скопируйте этот файл в .env.local для локальной разработки

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:4000

# Environment
NODE_ENV=development

# Добавьте свои переменные окружения здесь

# Карты (клиент) и геокодер (сервер). Для /api/geocode и /api/reverse-geocode
# Важно: для геокодирования (адрес → координаты) ключ должен иметь доступ к «Геокодер HTTP API»
# в кабинете https://developer.tech.yandex.ru (тип ключа: «JavaScript API и Геокодер HTTP API»).
# Ключ только для JavaScript API даёт 403 при запросах с сервера.
NEXT_PUBLIC_YANDEX_MAPS_API_KEY=
# Рекомендуется: отдельный ключ только для геокодера (серверный, не NEXT_PUBLIC)
# YANDEX_GEOCODER_API_KEY=
